{
  "name": "fubu-verify-bot",
  "version": "1.0.0",
  "private": true,
  "description": "FUBU Discord helper: archives+locks verification threads after a mod grants the Verified role; nudges mods about pending threads; auto-closes stale ones. It does NOT perform verification itself.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "discord.js": "^14.16.0"
  }
}
